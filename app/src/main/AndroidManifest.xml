<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.frank.wuhanjikong">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.login.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.test.TestDatabaseActivity" />
        <activity android:name=".ui.test.TestDbEntityActivity" />
        <activity android:name=".ui.home.HomeActivity" />
        <activity android:name=".ui.login.LoginActivity" />
        <activity android:name=".ui.home.VideoActivity" />
        <activity
            android:name=".ui.home.ApplicationLoad"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".ui.home.PersonalActivity" />
        <activity android:name=".ui.person.PersonDetailActivity" />
        <activity android:name=".ui.person.PersonMoneyActivity" />
        <activity android:name=".ui.person.HskActivity" />
        <activity android:name=".ui.person.PersonCollectionActivity" />
        <activity android:name=".ui.person.SettingActivity" />
        <activity android:name=".ui.person.ServerActivity" />
        <activity android:name=".ui.person.discoverActivity" />
        <activity android:name=".ui.person.mapdetailActivity" />
        <activity android:name=".ui.person.interestActivity"></activity>
    </application>

</manifest>